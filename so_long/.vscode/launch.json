{
	"version": "0.2.0",
	"configurations": [
		{
			"name": "Launch so_long",
			"type": "lldb",
			"request": "launch",
			"program": "${workspaceFolder}/so_long",
			"args": ["./src/maps/tiny.ber"],
			"cwd": "${workspaceFolder}",
			"preLaunchTask": "Make Rebuild",
			"console": "integratedTerminal"
		}
	]
}
